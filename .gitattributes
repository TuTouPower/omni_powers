# 跨平台：强制 LF 行尾（防 Windows clone CRLF 破坏 shebang + polyglot wrapper）
# superpowers 同样做法（CRLF 会破坏 : << 'CMDBLOCK' heredoc）
* text=auto eol=lf

# 显式 LF（脚本/配置敏感）
*.sh    text eol=lf
*.bats  text eol=lf
*.cmd   text eol=lf
*.json  text eol=lf
*.md    text eol=lf
*.sh.tmpl text eol=lf
