# 系统架构

> 职责（design §3.3）：**架构真相独占**——技术栈 + 目录结构 + 模块划分 + 数据流 + 跨模块契约。
> 不在此：命名/编码风格（→ conventions.md）、业务不变量（→ domain.md）。

## 架构总览
（图/文字描述系统组成）

## 模块划分
| 模块 | 职责 | 关键文件 |
|---|---|---|
| {模块名} | {职责} | {路径} |

## 目录结构
（项目目录树 + 关键目录说明——**唯一目录真相**，CLAUDE.md/spec_index 不重复，指向此处）

## 数据流
（请求/事件/任务从入口到出口的流转）

## 跨模块契约
| 契约 | 调用方 | 提供方 | 约束 |
|---|---|---|---|
| {契约名} | {模块} | {模块} | {输入/输出/错误约定} |

## 技术选型
（栈 + 选定理由，详见 decisions.md）

## 部署
（运行环境 / 构建发布 / 外部依赖；不适用项写“不适用”）
