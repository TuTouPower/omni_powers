# 系统架构

> 职责（design §3.3）：**架构真相独占**——技术栈 + 目录结构 + 模块划分 + 数据流 + 跨模块契约。
> 不在此：命名/编码风格（→ conventions.md）、业务不变量（→ domain.md）。

## 架构总览
（图/文字描述系统组成）

## 模块划分
| 模块 | 职责 | 关键文件 |
|---|---|---|
| Web | ... | src/web/ |
| API | ... | src/api/ |
| DB | ... | migrations/ |

## 目录结构
（src/ 树状结构 + 关键目录说明——**唯一目录真相**，CLAUDE.md/spec_index 不重复，指向此处）

## 数据流
（请求从入口到出口的流转）

## 技术选型
（栈 + 选定理由，详见 decisions.md）

## 部署
（compose / 容器 / 环境要求）
